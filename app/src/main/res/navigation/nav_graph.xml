<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/characters">

    <fragment
        android:id="@+id/characters"
        android:name="com.ahmedobied.ricknmorty.ui.characters.CharactersFragment"
        android:label="@string/characters"
        tools:layout="@layout/characters_fragment" />
    <fragment
        android:id="@+id/locations"
        android:name="com.ahmedobied.ricknmorty.ui.locations.LocationFragment"
        android:label="@string/locations"
        tools:layout="@layout/location_fragment" />
    <fragment
        android:id="@+id/episodes"
        android:name="com.ahmedobied.ricknmorty.ui.episodes.EpisodeFragment"
        android:label="@string/episodes"
        tools:layout="@layout/episode_fragment" />
</navigation>